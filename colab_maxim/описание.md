# Notebook Максима: заготовка для разведочного анализа

## Текущее состояние

`Razvedichni_analiz.ipynb` является валидным, но пустым Jupyter Notebook:

- кодовых ячеек — 0;
- markdown-ячеек — 0;
- входные данные не подключены;
- вычисления и визуализации отсутствуют;
- выходные файлы не создаются.

Поэтому на текущем состоянии невозможно достоверно описать модель, алгоритм или требования к колонкам dataset.

## Предполагаемое назначение

По имени файла notebook предназначен для разведочного анализа данных. Рекомендуемая структура будущей работы:

1. импорт `pandas`, `numpy`, `matplotlib` и `seaborn`;
2. загрузка нужного dataset из `../datasets/`;
3. проверка типов, пропусков и дубликатов;
4. описательная статистика;
5. анализ распределений и выбросов;
6. корреляционный анализ;
7. документирование выводов в markdown-ячейках.

## Данные

Общий dataset проекта находится здесь:

```text
../datasets/smolensk_dataset_shared.csv
```

Он содержит 80 строк и 35 столбцов. Подключать его следует только после определения задачи notebook и необходимого набора признаков.

Пример загрузки:

```python
import pandas as pd

df = pd.read_csv('../datasets/smolensk_dataset_shared.csv', sep=';')
```

## Запуск

Из корня проекта:

```powershell
.\.venv\Scripts\Activate.ps1
python -m jupyter lab --ServerApp.ip=127.0.0.1 --ServerApp.port=8888
```

## Что нужно определить перед разработкой

- цель разведочного анализа;
- целевой показатель;
- нужные столбцы и период наблюдений;
- правила обработки пропусков и выбросов;
- требуемые графики и итоговые артефакты;
- нужен ли отдельный Colab-updater, как у Кирилла и Гульдар.

После появления кода это описание необходимо обновить по фактической структуре notebook.
